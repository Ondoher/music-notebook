/** Props for the document message dialog host. */
type DocumentMessageDialogProps = {
	/** Document feature controller. */
	documentController: DocumentController;
};

/** State for the document message dialog host. */
type DocumentMessageDialogState = DocumentControllerDialogState;
